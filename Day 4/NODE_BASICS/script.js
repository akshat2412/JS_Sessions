const lib = require('./lib')
console.log('Hello')
lib.hello("akshat")


// every file in nodejs is a module
// every module runs in its own scope
// therefore you can write this.a = something            in any js file
